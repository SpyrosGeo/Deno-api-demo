### API list
-  **`GET`**-/todos
-  **`POST`**-/todos(from data)
-  **`PUT`**-/todos/:id (from data)
-  **`DETE`**-/todos/:id 

**Run**
```
deno run --allow-net --allow-read --allow-write ./index.js
``` 
